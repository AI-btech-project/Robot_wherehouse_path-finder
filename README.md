# Intelligent Warehouse Path Planner

An intelligent warehouse path planning and simulation application built with React and Vite.

## Run with Docker

To build the Docker image:

```bash
docker compose build
```

To start the application:

```bash
docker compose up
```

Once running, the application is available at:

http://localhost:3000

To stop the application:

```bash
docker compose down
```
